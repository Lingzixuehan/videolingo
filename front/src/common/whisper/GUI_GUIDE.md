# 🎨 GUI 应用使用指南

## ✅ GUI 已启动

Whisper 模块的图形化界面已经启动！

**窗口应该已经出现在您的屏幕上**

---

## 🖥️ GUI 主界面介绍

### 界面组成

```
┌─────────────────────────────────────────────┐
│  Whisper 字幕提取和翻译工具 (Tkinter GUI)  │
├─────────────────────────────────────────────┤
│                                             │
│  选择语言模型：[tiny▼]                      │
│  (tiny, base, small, medium, large)        │
│                                             │
│  选择视频文件：[...]                        │
│  文件路径：(显示选择的文件)                 │
│                                             │
│  状态：就绪                                 │
│                                             │
│  [提取并嵌入字幕] [导入本地字幕]            │
│                                             │
└─────────────────────────────────────────────┘
```

### 功能说明

| 组件 | 功能 | 说明 |
|------|------|------|
| 语言模型选择 | 选择 Whisper 模型 | tiny: 最快, large: 最准 |
| 视频文件选择 | 浏览和选择视频 | 支持 mp4, avi, mov 等 |
| 提取并嵌入字幕 | 主要功能 | Whisper 提取 + 翻译 + 嵌入 |
| 导入本地字幕 | 导入 SRT | 用于处理已有字幕 |
| 状态显示 | 实时反馈 | 显示处理进度 |

---

## 🚀 使用步骤

### 步骤 1️⃣ 选择模型

点击 "语言模型" 下拉菜单，选择合适的模型：

```
tiny    - 最快（几秒），准确率最低
base    - 推荐（快速 + 准确平衡）
small   - 中等（更准确）
medium  - 较慢（更准确）
large   - 最慢（最准确）
```

**建议：** 首次使用选择 `base` 模型

---

### 步骤 2️⃣ 选择视频文件

1. 点击 "..." 按钮打开文件浏览器
2. 选择想要处理的视频文件
3. 确认后路径会显示在 "文件路径" 标签中

**支持的格式：**
- MP4 (.mp4)
- AVI (.avi)  
- MOV (.mov)
- MKV (.mkv)
- 等其他常见视频格式

---

### 步骤 3️⃣ 开始处理

点击 "提取并嵌入字幕" 按钮，GUI 会自动：

1. ✅ 使用 Whisper 提取英文字幕
2. ✅ 使用有道翻译翻译为中文
3. ✅ 生成双语字幕
4. ✅ 将字幕嵌入视频
5. ✅ 提取词汇标注

**处理时间：** 取决于视频长度和模型大小
- tiny/base: 5-15 分钟
- small/medium: 15-30 分钟
- large: 30+ 分钟

---

### 步骤 4️⃣ 查看输出文件

处理完成后，在视频所在目录会生成：

| 文件 | 说明 |
|------|------|
| `video.srt` | 原始英文字幕 |
| `video-zh.srt` | 中文字幕 |
| `video-bi.srt` | 双语字幕 |
| `video-labels.json` | 词汇标注 |
| `video_with_subs.mp4` | 嵌入字幕的视频 |

---

## 💡 常见操作

### 操作 1: 导入已有字幕

如果您已经有 SRT 字幕文件：

1. 点击 "导入本地字幕" 按钮
2. 选择 .srt 文件
3. GUI 会自动：
   - 翻译字幕
   - 生成词汇标注
   - 嵌入字幕到视频

### 操作 2: 更改输出目录

所有输出文件默认存储在视频所在目录。若要自定义：
- 修改代码中的 `output_dir` 参数
- 或手动移动生成的文件

### 操作 3: 查看处理进度

实时状态显示在 "状态" 标签中：
- "就绪" - 等待操作
- "正在提取字幕..." - 处理中
- "提取完成" - 第一步完成
- "嵌入完成" - 全部完成

---

## 🎯 使用场景示例

### 场景 1: 处理一个视频

```
1. 模型: base (推荐)
2. 视频: D:\Videos\tutorial.mp4
3. 点击: 提取并嵌入字幕
4. 等待: 5-10 分钟
5. 查看: tutorial_with_subs.mp4
```

### 场景 2: 批量处理多个视频

```
重复以下步骤多次：
1. 选择不同的视频
2. 点击 "提取并嵌入字幕"
3. 处理完成后继续下一个
```

### 场景 3: 只想翻译字幕

```
1. 获取 SRT 字幕文件
2. 点击 "导入本地字幕"
3. 选择 SRT 文件
4. GUI 会生成翻译版本
```

---

## ⚙️ 高级设置

### 修改模型参数

编辑 `gui/whisper.py` 中的 `model_options` 列表：

```python
model_options = ["tiny", "base", "small", "medium", "large"]
```

### 修改输出目录

在 `whisper()` 函数中修改 `outpath`：

```python
outpath = "your_custom_path"  # 默认为视频所在目录
```

### 添加更多功能

GUI 使用 Tkinter，可以轻松添加：
- 字幕预览
- 词汇统计
- 难度评估
- 等等

---

## 🐛 故障排除

### 问题 1: GUI 不显示

**症状：** 启动后没有窗口出现

**解决方案：**
```bash
# 检查 Tkinter 是否安装
python -m tkinter

# 应该弹出测试窗口
```

### 问题 2: 视频文件路径错误

**症状：** 选择视频后出现错误

**解决方案：**
- 确保视频文件路径不含中文字符
- 或使用绝对路径
- 检查文件是否存在

### 问题 3: Whisper 模块找不到

**症状：** "ModuleNotFoundError: No module named 'whisper'"

**解决方案：**
```bash
# 安装 Whisper
pip install openai-whisper

# 或者
conda install -c conda-forge openai-whisper
```

### 问题 4: FFmpeg 找不到

**症状：** 嵌入字幕时出错

**解决方案：**
```bash
# 安装 FFmpeg
pip install ffmpeg-python

# 确保 ffmpeg 可执行文件在 PATH 中
ffmpeg -version
```

### 问题 5: 翻译 API 失败

**症状：** "翻译失败"

**解决方案：**
- 检查网络连接
- 有道 API Key 是否有效
- 等待几秒后重试

---

## 📊 性能参考

| 操作 | 视频长度 | 模型 | 耗时 |
|------|--------|------|------|
| 字幕提取 | 10分钟 | tiny | ~1分钟 |
| 字幕提取 | 10分钟 | base | ~3分钟 |
| 字幕翻译 | 10分钟 | - | ~1分钟 |
| 字幕嵌入 | 10分钟 | - | ~2分钟 |
| 总耗时 | 10分钟 | base | ~6分钟 |

**总耗时估计 = 视频时长 × (0.3-0.5) 倍**

---

## 🔗 相关文档

- [README.md](README.md) - 模块概览
- [QUICK_REFERENCE.md](QUICK_REFERENCE.md) - 快速参考
- [USAGE_GUIDE.md](USAGE_GUIDE.md) - 详细使用指南

---

## 💡 提示

### 优化处理速度

1. **选择合适的模型：**
   - tiny: 最快，准确率相对较低
   - base: 推荐（速度+准确平衡）
   - large: 最准但最慢

2. **使用 GPU 加速：**
   ```bash
   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
   ```

3. **批量处理：**
   - 准备多个视频文件
   - 逐个处理，充分利用硬件资源

### 质量优化

1. **字幕准确度：**
   - 选择 medium 或 large 模型
   - 清晰的音频效果更好

2. **翻译质量：**
   - 字幕质量好时翻译更准
   - 专业术语可能需要手工调整

---

## 📝 输出文件说明

### 字幕文件格式

**SRT 格式示例：**
```
1
00:00:01,000 --> 00:00:03,000
Hello, welcome to the tutorial.

2
00:00:03,000 --> 00:00:05,000
Today we'll learn about video processing.
```

### JSON 标注格式

```json
{
  "source": "video.srt",
  "blocks": [...],
  "word_map": {
    "hello": {
      "translation": "你好",
      "difficulty": "基础词汇",
      "is_new": false
    }
  }
}
```

---

## ✅ 快速验证

运行以下命令验证 GUI 环境：

```bash
# 1. 检查 Tkinter
python -m tkinter

# 2. 检查必要模块
python -c "import tkinter, ffmpeg, whisper; print('✅ All OK')"

# 3. 启动 GUI
python gui/whisper.py
```

---

**已完成：** GUI 应用已启动！
**状态：** ✅ 运行中
**下一步：** 选择视频文件并开始处理

